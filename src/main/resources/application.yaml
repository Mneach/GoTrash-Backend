server:
    port: 8080

spring:
  application:
      name: gotrash-backend
  datasource:
      url:
      username: test
      password: test
  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always